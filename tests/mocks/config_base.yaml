# Configuration base

# This string should not be altered
a_string: bar

# This string SHOULD be altered
another_string: foobar

# This array should be overriden in "config_override"
an_array:
    - one
    - two
    - three

# Use associative array:
assoc_array:
    foo: bar
    zuq: faz
